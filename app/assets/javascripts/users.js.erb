

function create(attr){
  console.log("hh");
  var x = attr;
  return fetch('http://localhost:3000/users', {
  method: 'POST',
  headers:{'Content-Type': 'application/json'}
  ,body: JSON.stringify(x)}).then(res=> res.json())
}

  function b(){

    return fetch('https://api.infermedica.com/v2/symptoms', {method: 'GET', headers:{'App_key': '3580-----e', 'App_Id': 'f---4' }}).then(res=> res.json())
    .then((x) => {fetch('/api/v1/symptoms', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json'
        }, body: JSON.stringify(x)
      }).then(res=> res.json())}
    );
  }
